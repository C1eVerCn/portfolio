services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chen-yihang-portfolio
    environment:
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
    ports:
      - "3000:3000"
    restart: unless-stopped
